doctrine:
  dbal:
    driver: 'pdo_sqlite'
    url: 'sqlite:///%kernel.project_dir%/var/test/test.db3'
  orm:
    mappings:
      commonEntities:
        type: yml
        dir: "%kernel.root_dir%/../config/doctrine"
        prefix: App\Entity
        is_bundle: false
